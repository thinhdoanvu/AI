nhapds(0, []).
nhapds(N, [Head|Tail]) :-
    N > 0,
    format('Phan tu thu ~w: ', [N]),
    read(Head),
    NewN is N - 1,
    nhapds(NewN, Tail).

print_list([]).
print_list([Head|Tail]) :-
    write(Head), write(' '),
    print_list(Tail).

tim_so_lon_nhat([K], X):- X=K. %hang nay bat buoc phai co
%
tim_so_lon_nhat([Head|Tail], Max) :-
    tim_so_lon_nhat(Tail, MaxTail),
    (Head > MaxTail -> Max = Head ; Max = MaxTail).

nhap_va_in_max(N) :-
    nhapds(N, List),
    write('In danh sach: '),
    print_list(List),
    tim_so_lon_nhat(List, Max),
    write('\n'),
    format('So lon nhat: ~w', [Max]).
    
    
% Quy t?c tim_so_lon_nhat([X], X) trong chuong trình Prolog du?c s? d?ng d? xác d?nh s? l?n nh?t trong m?t danh sách g?m m?t ph?n t? duy nh?t.
% C? th?, n?u danh sách ch? ch?a m?t ph?n t?, quy t?c tim_so_lon_nhat([X], X) s? du?c áp d?ng và s? l?n nh?t trong danh sách s? du?c gán cho bi?n X.
% Ví d?, n?u b?n g?i tim_so_lon_nhat([5], Max)., Prolog s? áp d?ng quy t?c tim_so_lon_nhat([X], X) và gán giá tr? 5 cho bi?n Max.

